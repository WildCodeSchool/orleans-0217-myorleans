{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typebien</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeBien.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ typeBien.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typebien_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typebien_edit', { 'id': typeBien.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
