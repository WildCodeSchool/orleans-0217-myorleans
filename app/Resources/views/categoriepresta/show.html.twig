{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categorieprestum</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categoriePrestum.id }}</td>
            </tr>
            <tr>
                <th>Nomcategorie</th>
                <td>{{ categoriePrestum.nomCategorie }}</td>
            </tr>
            <tr>
                <th>Ordreaffichage</th>
                <td>{{ categoriePrestum.ordreAffichage }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_categorie-presta_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_categorie-presta_edit', { 'id': categoriePrestum.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
