{% extends '::layout_admin.html.twig' %}

{% block content %}

    <div class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <div class="card-header light-green">
                            <h4 class="title"> Créer une nouvelle catégorie de prestation</h4>
                        </div>
                        <div class="card-content table-responsive">

                            {{ form_start(form) }}
                            {{ form_widget(form) }}
                            <input class="waves-effect waves-light btn-large light-green" type="submit" value="Créer"/>
                            {{ form_end(form) }}

                            <ul>
                                <li>
                                    <a href="{{ path('admin_categorie-presta_index') }}"><i class="material-icons">keyboard_return</i>Retour
                                        à la liste des catégories</a>
                                </li>
                            </ul>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

