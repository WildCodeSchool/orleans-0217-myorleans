{% extends 'base.html.twig' %}

{% block body %}
    <h1>Quartier</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quartier.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ quartier.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_quartier_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_quartier_edit', { 'id': quartier.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
