{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typeprestum</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typePrestum.id }}</td>
            </tr>
            <tr>
                <th>Nomtype</th>
                <td>{{ typePrestum.nomType }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_typepresta_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_typepresta_edit', { 'id': typePrestum.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
