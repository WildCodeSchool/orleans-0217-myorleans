{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typeprestum</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typePrestum.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ typePrestum.type }}</td>
            </tr>
            <tr>
                <th>Ordre</th>
                <td>{{ typePrestum.ordre }}</td>
            </tr>
            <tr>
                <th>Ordreaffichage</th>
                <td>{{ typePrestum.ordreAffichage }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typepresta_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typepresta_edit', { 'id': typePrestum.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
